# Secure Backup Script (Capstone)

Write a script that:

## Requirements

- Archives a directory
- Compresses it
- Encrypts it using GPG
- Uploads it to a remote server via SSH
- Verifies integrity after upload

## Concepts Covered

- `tar`
- `gzip`
- `gpg`
- `scp`
- Checksums

