# Git Configuration Auditor

Create a script that:

## Requirements

- Prints current global git config
- Validates:
  - `user.name`
  - `user.email`
  - `core.editor`
- Sets defaults if missing

## Concepts Covered

- `git config`
- Conditionals

