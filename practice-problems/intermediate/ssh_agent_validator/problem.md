# SSH Agent Validator

Write a script that:

## Requirements

- Checks if ssh-agent is running
- Starts it if not
- Verifies at least one key is loaded
- Prompts user to add a key if none exists

## Concepts Covered

- `ssh-agent`
- `ssh-add`
- Process detection

